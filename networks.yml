version: "2.1"

services:

  net_tests:
    image: alpine
    container_name:  net_tests
    command: echo "network installed"
    networks:
      rests_net:

networks:

  rests_net:
    driver: bridge
    ipam:
      driver: default
      config:
      - subnet: 10.10.0.0/16
        gateway: 10.10.0.1